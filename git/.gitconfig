[user]
	email = gr3y0shi@protonmail.com
	name = gr3y0shi
[color]
	ui = auto
[alias]
	co = checkout
	fa = fetch -a
	br = branch
	ci = commit
	cim = commit -m
	civ = commit -v
	sw = switch
	st = status
	unstage = reset HEAD --
	lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
	sh = show
